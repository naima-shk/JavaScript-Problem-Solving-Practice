//Create a variable and display it on the webpage. Add a button on the webpage. Create a function that runs on every click of the button. Whenever the function is executed the value of the variable will be increased by 5.
let num = 999;
document.write(num); 

let  n = 0;
function increment(){
    n=n+5;
    console.log(n);
    return n;
}
