// 3-Create a function called myFunc and declare a variable name inside the body of that function. In the console check the value of the name and see what console prints.
function myFunc(){
    let number;
    console.log(number);
}